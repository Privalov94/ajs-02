"use strict";

var ajs = require('../..');
var str = "<% posts.forEach(function (c) { -%>\n    <%= c -%>\ns are great\n<% }) %>";

ajs.render(str, {
    filename: "foo.js",
    locals: {
        posts: ["Apple", "Pear", "Orange"]
    }
}, function (err, data) {
    console.log(err || data);
});